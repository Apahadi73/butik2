apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-postgres
  template:
    metadata:
      labels:
        app: products-postgres
    spec:
      containers:
        - name: products-postgres
          image: postgres
---
apiVersion: v1
kind: Service
metadata:
  name: products-postgres-srv
spec:
  selector:
    app: products-postgres
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
